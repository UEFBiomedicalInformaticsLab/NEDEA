# Extract the RWR statistics
rwr_stats <- statisticsRWR(rwr_result)


# hist(rwr_stats$IQRs, main = "knonw drug targets - IQRs", col = "blue")
# hist(rwr_stats$OUTs, main = "knonw drug targets - OUTs", col = "lightblue")
